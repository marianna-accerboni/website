---
import { render } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getExhibitions } from '../../lib/exhibitions';
import { href } from '../../lib/url';

export async function getStaticPaths() {
  const exhibitions = await getExhibitions();
  return exhibitions.map((entry) => ({
    params: { slug: entry.data.slug },
    props: { entry }
  }));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
const { title, artists, venue, city, startDate, endDate, coverImage, gallery, attachments, videos } = entry.data;

const formatDate = (d: Date) =>
  d.toLocaleDateString('it-IT', { day: 'numeric', month: 'long', year: 'numeric' });
---

<BaseLayout title={title}>
  <article class="exhibition">
    <h1>{title}</h1>
    <p class="meta">
      <strong>Artisti:</strong> {artists.join(', ')}<br />
      <strong>Sede:</strong> {venue}, {city}<br />
      <strong>Date:</strong>
      Dal {formatDate(startDate)}
      {endDate && <span> al {formatDate(endDate)}</span>}
    </p>

    {coverImage && <img class="cover" src={href(coverImage)} alt={title} />}

    <div class="content">
      <Content />
    </div>

    {
      gallery.length > 0 && (
        <section>
          <h2>Galleria</h2>
          <div class="gallery">
            {gallery.map((img) => (
              <img src={href(img)} alt={title} loading="lazy" />
            ))}
          </div>
        </section>
      )
    }

    {
      videos.length > 0 && (
        <section>
          <h2>Video</h2>
          {videos.map((url) => (
            <div class="video-wrapper">
              <iframe src={url} title={title} allowfullscreen loading="lazy" />
            </div>
          ))}
        </section>
      )
    }

    {
      attachments.length > 0 && (
        <section>
          <h2>Allegati</h2>
          <ul>
            {attachments.map((a) => (
              <li>
                <a href={href(a.file)} target="_blank" rel="noopener">{a.label}</a>
              </li>
            ))}
          </ul>
        </section>
      )
    }

    <p><a href={href('/')}>&larr; Torna alle mostre</a></p>
  </article>
</BaseLayout>

<style>
  .exhibition .meta {
    color: var(--color-muted, #666);
  }

  .cover {
    border-radius: 6px;
    margin: 1rem 0;
  }

  .content {
    max-width: 46rem;
  }

  .gallery {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 1rem;
  }

  .gallery img {
    border-radius: 6px;
    display: block;
  }

  .video-wrapper {
    position: relative;
    aspect-ratio: 16 / 9;
    max-width: 46rem;
  }

  .video-wrapper iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border: 0;
  }
</style>
